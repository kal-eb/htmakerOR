'use strict';

/**
 * @ngdoc function
 * @name htmakerOrApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the htmakerOrApp
 */
angular.module('htmakerOrApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
